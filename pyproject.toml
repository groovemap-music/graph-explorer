[build-system]
requires = ["hatchling>=1.27.0"]
build-backend = "hatchling.build"

[project]
name = "groovemap-graph-explorer"
version = "0.1.0"
description = "Public graph exploration web application and Catalog API proxy for GrooveMap"
authors = [{name = "Robert Wlodarczyk", email = "robert@simplicityguy.com"}]
readme = "README.md"
requires-python = ">=3.13"
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: End Users/Desktop",
    "License :: OSI Approved :: GNU Affero General Public License v3",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.13",
]
license = "AGPL-3.0-only"
license-files = ["LICENSE", "NOTICE", "COMMERCIAL-LICENSING.md", "BRAND-NOTICE.md"]
dependencies = [
    "fastapi>=0.141.1",
    "groovemap-runtime==0.1.0",
    "httpx>=0.28.1",
    "orjson>=3.12.0",
    "pydantic>=2.13.4",
    "python-multipart>=0.0.32",
    "structlog>=26.1.0",
    "uvicorn[standard]>=0.52.4",
]

[project.scripts]
graph-explorer = "explore.explore:main"

[project.urls]
Homepage = "https://groovemap.music"
Repository = "https://github.com/groovemap-music/graph-explorer"
Issues = "https://github.com/groovemap-music/graph-explorer/issues"

[dependency-groups]
dev = [
    "build>=1.3.0",
    "commitizen>=4.18.0",
    "cyclonedx-bom>=7.3.1",
    "mypy>=2.3.1",
    "pip-audit>=2.10.1",
    "pip-licenses>=5.5.5",
    "playwright>=1.62.0",
    "pytest>=9.1.1",
    "pytest-asyncio>=1.4.0",
    "pytest-base-url>=2.1.0",
    "pytest-cov>=7.1.0",
    "pytest-playwright>=0.9.0",
    "pytest-timeout>=2.4.0",
    "pytest-xdist>=3.8.0",
    "ruff>=0.16.4",
]

[tool.hatch.build.targets.wheel]
packages = ["explore"]

[tool.hatch.build]
artifacts = ["explore/static/tailwind.css", "explore/static/vendor"]

[tool.uv]
exclude-newer = "2 days"

[tool.uv.sources]
groovemap-runtime = { git = "https://github.com/groovemap-music/python-libraries.git", rev = "28fa329702bc76896cc54ab8d05ec5b1bd3d929e" }

[tool.ruff]
line-length = 150
target-version = "py313"

[tool.ruff.lint]
select = ["ARG", "B", "C4", "E", "F", "I", "PLC", "PTH", "RUF", "S", "SIM", "T20", "TCH", "UP", "W"]
ignore = ["B008", "C901", "E501", "S101"]

[tool.ruff.lint.isort]
known-first-party = ["explore"]
lines-after-imports = 2

[tool.ruff.lint.per-file-ignores]
"explore/explore.py" = ["T201"]
"tests/**/*.py" = ["ARG", "PLC", "S105", "S106", "T201"]
"scripts/*.py" = ["S108", "T201"]

[tool.mypy]
python_version = "3.13"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
disallow_incomplete_defs = true
check_untyped_defs = true
no_implicit_optional = true
warn_redundant_casts = true
warn_unused_ignores = true
warn_no_return = true
warn_unreachable = true
strict_equality = true
packages = ["explore"]

[tool.pytest.ini_options]
addopts = "-ra -q --strict-markers --tb=short"
testpaths = ["tests"]
asyncio_mode = "strict"
asyncio_default_fixture_loop_scope = "function"
timeout = 60
timeout_method = "thread"
markers = [
    "benchmark: performance regression benchmarks",
    "e2e: browser-based end-to-end tests",
    "e2e: end-to-end tests requiring external services",
    "integration: tests requiring a live backing service",
]

[tool.coverage.run]
source = ["explore"]
omit = ["*/tests/*", "*/__init__.py"]

[tool.coverage.report]
exclude_lines = [
    "def __repr__",
    "if __name__ == .__main__.:",
    "pragma: no cover",
    "raise AssertionError",
    "raise NotImplementedError",
]

[tool.commitizen]
name = "cz_conventional_commits"
version_provider = "uv"
version_scheme = "pep440"
version_files = ["explore/__init__.py:__version__"]
tag_format = "v$version"
annotated_tag = true
update_changelog_on_bump = true
changelog_file = "CHANGELOG.md"
